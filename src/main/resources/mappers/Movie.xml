<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SE_Dao">
<select id="seMovieListAll" resultType="Movie">
select * from Movie
</select>

<select id="seMovieSelOne" parameterType="int" resultType="Movie">
select * from movie where mo_number= #{mo_number}
</select>

<select id="seFindLikeSelOne" parameterType="java.util.HashMap" resultType="MovieLike">
	select count(*) from movieLike where mo_number=#{mo_number} and m_id=${m_id}
</select>

<insert id="seInsertLike" parameterType="java.util.HashMap">
	insert into movielike values(${m_id},#{mo_number})
</insert>

<update id="seUpdateMoiveLike" parameterType="java.util.HashMap">
	update movie set mo_recommendation = mo_recommendation+1 where mo_number=#{mo_number}
</update>

<delete id="seDeleteLike" parameterType="java.util.HashMap">
DELETE FROM movielike
           WHERE mo_number=#{mo_number} and m_id=${m_id}
</delete>

<update id="seUpdateMoiveLikeCancle" parameterType="java.util.HashMap">
	update movie set mo_recommendation = mo_recommendation-1 where mo_number=#{mo_number}
</update>
</mapper>